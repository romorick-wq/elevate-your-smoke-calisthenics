{
  "name": "elevate-your-smoke",
  "version": "1.1.0",
  "private": true,
  "description": "Elevate Your Smoke — calisthenics challenge + roster API",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "install:backend": "npm install --prefix backend",
    "test": "node backend/test/score.test.js && node backend/test/workout.test.js && node backend/test/copy-privacy-delete.test.js",
    "check": "node --check backend/server.js && node --check backend/db.js && node --check backend/score.js && node --check backend/sms.js && node --check backend/workout.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.3"
  }
}
